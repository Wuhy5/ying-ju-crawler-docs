# JSON Schema

以下是媒体爬虫规范的 JSON Schema 定义：[schema.json](./schema.json)

这个 JSON Schema 定义了媒体爬虫规则文件的完整结构，包括元数据、配置、组件、流程等部分，用于验证规则文件的有效性。

参考 [JSON Schema 官方文档](https://json-schema.org/) 了解更多关于 JSON Schema 的信息。
